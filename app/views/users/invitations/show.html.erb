<div class="container-fluid">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <h4>招待用URL作成</h4>
        <%= text_field_tag :invitation_token_url, @invitation_token_url, class: 'form-control' %>
        <div class="margin-top-30">
          <%= button_to 'URLを再作成する', users_invitations_path, class: 'btn btn-primary', form: { "data-turbo-action": "replace" } %>
        </div>
      </div>
    </div>
  </div>
</div>
