<%= form_with(model: @group, local: true) do |f| %>

  <div class="title">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="submit">
    <%= f.submit %>
  </div>

<% end %>