<div class="col-md-9 mx-auto text-center mt-5 text-gray-600">
    <h1 class="text-2xl"><%= @group.title %>グループの参加申請一覧</h1>
    <p class="text-muted">参加申請を許可するか拒否するかを選択してください</p>
    <div class="mt-5 flex flex-wrap justify-center">
      <% @permits.each do |permit| %>
        <div class="card card-side bg-base-100 shadow-xl w-1/2 md:w-1/3 lg:w-1/4 xl:w-1/4 m-2">
          <div class="flex justify-center">
          <figure class="w-3/4 "><%= image_tag permit.user.profile_image.present? ? user.profile_image.url : "default.jpg"%></figure>
          </div>
          <div class="card-body">
            <h2 class="card-title"><%= permit.user.name %></h2>
            <p class="m-1"><%= permit.user.introduction %></p>
            <div class="card-actions">
              <%= link_to '許可', group_group_users_path(@group, permit_id: permit.id), data: { turbo_method: :post }, class: "rounded-lg drop-shadow-lg border-2 bg-primary text-secondary hover:bg-accent hover:drop-shadow-none px-2 py-1" %>
              <%= link_to '拒否', group_group_users_path(@group, permit_id: permit.id), data: { turbo_method: :delete }, class: "text-primary rounded-lg border-2 border-primary hover:bg-gray-200 px-2 py-1" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <p class="text-center mt-10"><%= link_to "戻る", group_path(@group) %></p>
</div>
